# 2026.246.2119 — feat: bump openclaw to the latest stable 2026.9.1

## Summary

Bump the openclaw npm package from `2026.8.2` (the 2.0 patch) to `2026.9.1` —
the current `latest` dist-tag on npm (a follow-up to OpenClaw 2.0 with Mermaid
diagram rendering, a fuller Android experience, safer update recovery, and gateway
restart recovery; no gateway-startup breaking changes). The supervised exec line,
the `gateway.mode=local` guard, and the `~/.openclaw/openclaw.json` config all
carry over unchanged.

What changes:

- **package.json** — `openclaw` dependency `2026.8.2` → `2026.9.1`.
- **plan** — the version check now matches `2026.9.1`.
- **description** — prose updated for the current version.

## How tested

### 1. `charly box validate` (the config gate)

```
$ /tmp/charly-ci/bin/charly box validate
exit 0
```

Run with the charly version this repo's own `deploy.yml` clones and builds
(v2026.238.1242).

### 2. The `check-openclaw-pod` bed run (the R10 proof — the new version installs, starts, and passes `/healthz`)

The consuming bed builds the openclaw box (which composes this candy) and runs the
candy's plan checks. Run with `CHARLY_REPO_OVERRIDE` pointing `pod-openclaw` at
this PR's fixed checkout (the standard dev-tool for proving an uncommitted candy
change before it is tagged):

```
$ charly check run check-openclaw-pod
charly check run check-openclaw-pod: [image-build] PASS [warnings=1 allowlisted=11 cache=117/181]
charly check run check-openclaw-pod: [check-image] PASS after 33.768s [allowlisted=1]
charly check run check-openclaw-pod: [deploy-add] PASS [allowlisted=1]
charly check run check-openclaw-pod: [config] PASS after 41.94s [warnings=3 allowlisted=1]
charly check run check-openclaw-pod: [start] PASS after 20.665s [warnings=1 allowlisted=1]
charly check run check-openclaw-pod: [bring-up-members] PASS after 54ms
charly check run check-openclaw-pod: [check-live] PASS after 3m59.83s [warnings=2 allowlisted=1]
charly check run check-openclaw-pod: [feature-run] PASS after 1m41.853s [warnings=2 allowlisted=1]
charly check run check-openclaw-pod: [update] PASS after 1m54.188s [warnings=9 allowlisted=5]
charly check run check-openclaw-pod: [check-live-rebuild] PASS after 4m40.763s [warnings=2 allowlisted=1]
charly check run check-openclaw-pod: [feature-run-rebuild] PASS after 3m53.747s [warnings=2 allowlisted=1]
charly check run check-openclaw-pod: [cleanup] PASS after 33.708s [allowlisted=1]
charly check run check-openclaw-pod: [cleanup-members] PASS after 0s
charly check run check-openclaw-pod: PASS (steps=13)
```

The `check-live` step includes the version check and the `/healthz` probe against
the running 2026.9.1 gateway:

```
PASS  check the openclaw gateway reports the exact security-patched package version  [plan:candy:github.com/opencharly/pod-openclaw:1]  exit=0
PASS  check the running gateway's /healthz liveness endpoint responds 200 (OpenClaw 2.0 container probe)  [plan:candy:github.com/opencharly/pod-openclaw:2]  status=200
PASS  check the openclaw verb reaches the gateway's /healthz liveness endpoint on the running 2.0 gateway  [openclaw-verb-health]  {"ok":true,"status":"live"}
PASS  check the openclaw verb reports the gateway ready via /readyz  [openclaw-verb-ready]  {"ready":true,"failing":[],"uptimeMs":236675,...}
PASS  check the openclaw verb reports the gateway's model status via the in-venue CLI  [openclaw-verb-models]  Config: ~/.openclaw/openclaw.json
```

The `warnings` counts are the allowlisted host package-state class
(pacman-contrib/zstd local-newer-than-repo) and the podman store bloat host
condition — both unrelated to this version bump.

## Rulebook compliance

- **R0 (skills)** - `/charly-image:layer` for the candy/plan model;
  `/charly-internals:git-workflow` for PR-only landing and tag-on-merge.
- **R1 (RCA)** - the version was verified against the npm registry (2026.9.1 is the
  `latest` dist-tag); the gateway startup surface was verified unchanged in 2026.9.1.
- **R2 (no deferral)** - the bump ships now.
- **R3 (no duplication)** - version pinned in `package.json` only; the plan check
  reads the installed binary's `--version`.
- **R4 (no workarounds)** - N/A.
- **R5 (hard cutover)** - `2026.8.2` is gone; no old path remains.
- **R6 (git safety)** - feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** - `charly box validate` on the committed tree AND
  the `check-openclaw-pod` bed run (PASS, steps=13) with the verbatim output above,
  exercising the new version's install/start/`/healthz` path.
- **R8 (generation)** - N/A: no generated artifact, no CUE input in this repo.
- **R9 (artifact integrity)** - N/A: no image is built here.
- **ADE (candy description/plan)** - `description:` and `plan:` updated in lockstep
  with the change they describe.
- **SDD (CUE source)** - N/A: no schema authored or generated here.

## Change classification

- **Change class:** candy dependency version bump + plan/prose updates. Config only.
- **Verification gate:** `charly box validate` + the `check-openclaw-pod` bed run
  (PASS, steps=13, verbatim output above)
- **Attribution tier:** `fully tested and validated` - the bed ran live with the
  new version installed, started, and `/healthz`-probed, with the verbatim verdict
  recorded

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*